# self关键字: 谁调用run函数,self就是谁
# 做实验: 两个不同的对象 self是不一样的

# 1 定义类
class Car:
    # 2 定义类的方法
    def run(self):

        print(f"能跑起来...{self}")


# 3 创建对象
# 3.1 创建具体的对象
mycar01 = Car()
# 3.2 调用类的方法
print("mycar01-->", mycar01)
mycar01.run()


# 4 再创建对象2
mycar02 = Car()
print("mycar02-->", mycar02)
# 3.2 调用类的方法
mycar02.run()



# self关键字的作用: 在类的内部,调用类的方法
#               语法: 类的内部 self.方法()


# 1 定义类
class Car:
    # 2 定义类的方法
    def run(self):

        print(f"能跑起来...{self}")

    def work(self):
        self.run()


# 3 创建对象
# 3.1 创建具体的对象
mycar01 = Car()
# 3.2 调用类的方法

mycar01.work()

"""
在上述课堂实录中，讲解了Python中的`self`关键字及其原理、意义、用法和注意事项。以下是整理出的关键点：

### 1. **`self`的原理与意义**

* `self`是Python中的一个内置关键字，用于在类的方法中引用当前对象实例。它用于区分不同对象调用类方法时，哪个对象是当前的调用者。
* 在类中定义的方法，通常是通过对象来调用的，而`self`表示当前实例（对象）本身。因此，`self`帮助我们区分多个对象在调用相同类方法时，指向的具体实例。
* 它的核心作用是解决类和对象之间的“一对多”关系：多个对象可以共享同一个类方法，但每个对象都能通过`self`指向自身，确保操作的是自己相关的属性或方法。

### 2. **`self`的用法**

* 在定义类的方法时，第一个参数通常是`self`。这个参数并不是显式传入的，而是由Python解释器隐式传递给方法。当你通过对象调用方法时，`self`会自动指向该对象。
* 示例代码中，当我们调用类的方法时，Python会自动传递调用该方法的对象给`self`，因此我们可以通过`self`访问当前对象的属性和方法。

  ```python
  class Car:
      def run(self):
          print(f'{self} is running')

  car1 = Car()
  car2 = Car()
  car1.run()  # self 指向 car1
  car2.run()  # self 指向 car2
  ```

### 3. **注意事项**

* `self`仅用于实例方法中，它必须是方法的第一个参数。这个参数是自动传入的，不需要在调用时手动传递。
* 虽然`self`是方法的第一个参数，但在调用时，我们并不需要显式地传递它。Python解释器会自动将调用方法的对象作为`self`的值传递。
* `self`并不是一个固定的关键字，你可以使用其他名字代替它，但遵循使用`self`作为惯例，有助于代码的可读性和理解。
* 在实验中，`self`的关键作用是确定是哪一个对象调用了方法。通过打印对象的内存地址或使用`id(self)`，可以确认每个对象调用方法时的`self`是独立的。

### 4. **进一步的理解**

* 在Python中，类和对象之间的关系通过`self`得到了有效的管理。`self`的出现解决了如何区分不同对象调用同一方法的难题。程序通过`self`来确保每个对象能独立地操作自己的属性和方法。

总结而言，`self`是Python面向对象编程中非常重要的机制，它使得类的设计能够灵活地处理多个实例，并确保每个实例都有自己的方法和属性的访问权限。

"""



