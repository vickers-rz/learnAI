`dtype` 是 Pandas 里用来表示 **每一列（Series）的数据类型** 的属性，它在 `DataFrame` 中起着非常重要的作用，因为 Pandas 在底层是基于 NumPy 数组的，不同的数据类型会直接影响性能、内存使用以及运算行为。下面我给你系统讲讲怎么用。

------

### 🧩 一、查看 dtype

最常见的方式：

```
import pandas as pd

df = pd.DataFrame({
    'age': [25, 30, 35],
    'name': ['Alice', 'Bob', 'Charlie'],
    'salary': [50000.0, 60000.0, 70000.0]
})

print(df.dtypes)
```

输出：

```
age          int64
name        object
salary     float64
dtype: object
```

解释：

- `int64`：整型
- `float64`：浮点型
- `object`：一般表示字符串或混合类型

------

### 🔍 二、指定 dtype（创建时）

你可以在创建 DataFrame 时就指定类型：

```
df = pd.DataFrame({
    'age': [25, 30, 35],
    'salary': [50000, 60000, 70000]
}, dtype='float32')
```

这样所有列都会是 `float32` 类型。

或者为每列单独指定：

```
df = pd.DataFrame({
    'age': pd.Series([25, 30, 35], dtype='int8'),
    'salary': pd.Series([50000, 60000, 70000], dtype='float32')
})
```

------

### 🔄 三、修改 dtype（转换类型）

用 `.astype()`：

```
df['age'] = df['age'].astype('float32')
df = df.astype({'salary': 'int32', 'name': 'string'})
```

如果类型不兼容会报错，可以加参数 `errors='ignore'` 或 `errors='coerce'`：

```
pd.to_numeric(df['age'], errors='coerce')
```

`'coerce'` 会把无法转换的值变成 `NaN`。

------

### ⚙️ 四、常见 dtype 类型

| 类型                     | 含义                                       |
| ------------------------ | ------------------------------------------ |
| `int64`, `int32`, `int8` | 整型，不同大小影响内存                     |
| `float64`, `float32`     | 浮点型                                     |
| `bool`                   | 布尔型                                     |
| `object`                 | 混合型（一般是字符串）                     |
| `category`               | 分类类型（节省内存、加速分组）             |
| `datetime64[ns]`         | 日期时间类型                               |
| `timedelta[ns]`          | 时间差类型                                 |
| `string`                 | 新的 Pandas 字符串类型（比 object 更稳定） |

