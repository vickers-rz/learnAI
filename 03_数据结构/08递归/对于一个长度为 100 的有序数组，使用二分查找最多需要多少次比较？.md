### 对于一个长度为 100 的有序数组，使用二分查找最多需要多少次比较？

二分查找的时间复杂度是 O(log n)，每次都将查找区间减半。因此，最坏情况下，查找次数就是将数组长度从 100 缩小到 1 所需的次数。

计算最多比较次数，可以使用以下公式：

​			$比较次数 = \lceil \log_2 100 \rceil$

其中 $n=100$。

计算过程：

​			$\log_2 100 \approx 6.64$

所以，最坏情况下需要进行 **7 次**比较。

因此，正确答案是 **C. 7**。
